# TODO — everything-flow

> 🔴 = 紧急 &nbsp; 🟠 = 高 &nbsp; 🟡 = 中 &nbsp; 🟢 = 低
> 
> ★☆☆☆☆ = 简单 &nbsp; ★★★★☆ = 复杂 &nbsp; ★★★★★ = 最复杂

---

## 🐛 Bug

- [x] ~~**快捷键面板被节点连接点遮挡** 🔴 ★☆☆☆☆
  键盘快捷键对话框（tldraw 原生）z-index 低于 ConnectorOverlay(10000)，已修复：CSS 覆盖 `.tlui-dialog__overlay` → z-index: 10010~~

- [x] ~~**节点缩小后出现无效滚动条** 🔴 ★★☆☆☆
  `overflow: auto` 导致节点缩小时出现滚动条，但 tldraw 接管事件导致滚动条不可用，改为 overlay 层自定义滚动条（通过 ConnectorOverlay，zIndex 10001）~~

- [x] ~~**多页面隔离冲突** 🔴 ★★☆☆☆
  添加新页面后，页面之间数据未完全隔离（tldraw 原生多页面），已修复：shape 查询按当前页面过滤 + connectionsRef 改为 page-scoped Map~~

- [x] ~~**连接线Al->Br箭头只有两段显示不全**~~
  链接线为Al->Br的时候会显示不全连接线，只有左，下两端
  **根因**: `highExt(b, a)` 中 `b` 是点对象 `{x,y}` 而非 bounds，`b.w` 为 undefined 导致 `NaN`，5段路由的 `ch` 为 NaN，路径到 NaN 处截断。修复：去掉 `b.w`/`b.h` 引用，直接用 `b.x + EXT` / `b.y + EXT`。

---

## P0 — ✅ 已完成

- [x] ~~**重做左上角面板** 🔴 ★★★★☆~~ ✅
  1:1 复刻 tldraw 左上角所有功能（MainMenu/PageMenu/MenuPanel），逐项验收后隐藏原生组件。
  Plan: `plans/approved/redesign-top-left-panel.md`
  - [x] Phase 0 — 截取当前 UI 参考图
  - [x] ~~Phase 1 — 汉堡菜单外壳（CustomMainMenu 骨架）~~ ✅
  - [x] ~~Phase 2.1 — 剪贴板组（剪切/复制/粘贴）~~ ✅
  - [x] ~~Phase 2.2 — 选择与编组（全选/编组/取消编组）~~ ✅
  - [x] ~~Phase 2.3 — 锁定组（锁定/解锁/全部解锁）~~ ✅
  - [ ] Phase 3 — ~~插入组（嵌入 + 媒体）~~ 🔄 推迟讨论（插入图片/文件的需求已记录）
  - [x] ~~Phase 4.1 — 偏好开关（参考线/网格/隐藏菜单/边缘滚动/粘贴到光标/调试模式）~~ ✅
  - [x] ~~Phase 4.2 — 主题选择（浅色/深色/跟随系统）~~ ✅
  - [ ] Phase 4.3 — 语言选择（下拉列表）🔄 推迟：需要实现 i18n 翻译系统，让自定义菜单/工具栏跟随语言切换
  - [x] ~~Phase 5 — 帮助组（快捷键面板 + 文档链接）~~ ✅
  - [x] ~~Phase 6 — 页面管理（CustomPageSelector）~~ ✅
  - [x] ~~Phase 7 — Undo/Redo 提取到顶部栏~~ ✅
  - [x] ~~Phase 8 — 💾 按钮改为导出下拉菜单~~ ✅
  - [ ] ~~Phase 9 — 布局重排~~ 🗑 跳过
  - [ ] ~~Phase 10 — 最终收尾~~ 🗑 跳过

---

## P1 — 核心功能

- [ ] **框选同时选中箭头** 🟠 ★★★★☆
  - 矩形选框（框选）时，在框内的箭头也被选中，可批量删除/切换模式
  - 需要实现箭头在 tldraw 形状系统中的命中检测

- [x] ~~**箭头不盖住面板** 🔴 ★★☆☆☆~~
  ConnectorOverlay z-index 从 9999 降至 150，低于 tldraw UI 面板

- [ ] **节点样式多样化（按标准流程图种类）** 🟠 ★★★★☆
  - [ ] 开始/结束（圆角胶囊形）
  - [ ] 处理（矩形）
  - [ ] 判断（菱形）
  - [ ] 子流程（双边矩形）
  - [ ] 文档（波浪底矩形）
  - [ ] 数据（平行四边形）
  - [ ] 每个类型绑定默认图标和配色
  - [x] ~~正交同侧：tt/bb/ll/rr 四方向，3段/5段~~ ✅
  - [x] ~~正交对角：交叉方向路由（top→left, bottom→right 等 8 方向），4段/2段~~ ✅
  - [x] ~~每条线可单独切换正交/直线~~
  - [ ] 全局默认连线模式设置
  > 验证原型：`docs/validation-prototypes/orthogonal-cross-all.html`

- [x] ~~**重新设计底部工具栏** 🟠 ★★☆☆☆~~
  隐藏 tldraw 默认工具栏，替换为自定义工具栏，仅保留：选择、手型、便签、图片、激光笔 5 个工具~~

- [x] ~~**重做左上角面板** 🟠 ★★☆☆☆
  重新设计左上角区域，替换 tldraw 默认菜单/页面管理~~

- [ ] **重新设计右上角调色盘** 🟠 ★★☆☆☆
  - [x] ~~隐藏 tldraw 默认样式面板~~（`StylePanel: null`）
  - [ ] 设计并实现自定义样式面板（整合节点背景色、边框色）

- [ ] **修复保存/加载方式** 🟠 ★★★☆☆
  - [ ] 优化 `.json` 导出（元数据、文件命名）
  - [ ] 实现 `.png` 导出（画布截图）
  - [ ] 实现 `.eflow` 专属格式（JSON + 版本号/时间戳/应用版本）
  - [ ] 加载时自动识别 `.json` / `.eflow` 格式
  - [ ] 保存/加载按钮合并为统一文件操作入口

- [ ] **节点分区分组及可视化** 🟡 ★★★★☆
  - [ ] 分组/泳道容器 Shape
  - [ ] 组背景色和标签
  - [ ] 子节点归属管理（拖入/拖出分组）

- [ ] **箭头样式自定义** 🟡 ★★☆☆☆
  - [ ] 颜色、粗细、线型（实线/虚线）UI 控制
  - [ ] 箭头头形状（三角形/圆形/菱形）
  - [ ] 正交模式下每段线长度调节

- [x] **节点间对齐模式** 🟡 ★★★★☆
  - [x] 多选后对齐操作（左/右/水平居中/顶部/底部/垂直居中）
  - [x] 等间距分布

- [ ] **节点选中状态优化** 🟡 ★★☆☆☆
  - [ ] 选中时其他节点变灰/变暗
  - [ ] （可选）设置中开启关联节点同步高亮

---

## P2 — 视觉与样式

- [ ] **节点背景颜色** 🟡 ★★☆☆☆
  - [ ] 节点 props 增加 `backgroundColor` 字段
  - [ ] 颜色选择器 UI（集成到调色盘）
  - [ ] 渲染时应用背景色

- [ ] **节点框颜色** 🟡 ★☆☆☆☆
  - [ ] 节点 props 增加 `borderColor` / `borderWidth` 字段
  - [ ] 渲染时应用边框样式

- [ ] **节点默认设置** 🟡 ★★★☆☆
  - [ ] 存储默认样式配置（localStorage）
  - [ ] 工具栏设置入口 UI
  - [ ] 新建节点时应用默认配置

---

## P3 — 增强功能

- [ ] **插入图片/文件** 🟢 ★★★★☆
  - [ ] 通过 URL 链接插入
  - [ ] 通过本地路径插入
  - [ ] 粘贴上传
  - [ ] 图片内嵌渲染 / 仅显示引用链接

---

## P4 — 工程整洁

- [x] ~~删除备份文件 `src/App.jsx.bak.v0.1`~~
- [ ] **源码结构整理** 🟢 ★★☆☆☆
  - [ ] Bridges 移入 `src/bridges/`（ActionsBridge, DialogBridge）
  - [ ] App.jsx 拆分：ConnectorOverlay, CustomToolbar, AlignmentButtons 提为独立组件
