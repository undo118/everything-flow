# Everything Flow — 自检流程

> 交付日期: 2026-06-02
> 项目目录: `~/hermes_workspace/projects/everything-flow/`
> 启动方式: 双击 `run.sh`（或 `run.bat`）

---

## 1. 环境自检

**启动前确认：**

- [ ] Node.js 已安装（`node -v` 显示版本号）
- [ ] 没有其他程序占用端口 3000

**如缺失，run.sh 会自动提示：**
- 无 Node.js → 显示下载链接，退出
- npm install 失败 → 提示换镜像源
- 端口被占 → 自动 +1 重试，最多 3 次

---

## 2. 启动验证

- [ ] 双击 `run.sh`（或 `run.bat`）
- [ ] 浏览器自动打开 `http://localhost:3000`
- [ ] 看到深色工具栏 + tldraw 画布（默认有箭头工具、形状工具的工具栏）
- [ ] 控制台无 JS 报错（F12 → Console）

**如果浏览器未自动打开：** 手动访问 `http://localhost:3000`

---

## 3. 功能逐项验证

### 3.1 节点创建 ✅

| 步骤 | 预期结果 | 实际 |
|------|---------|------|
| 点击工具栏「➕ 添加节点」 | 画布中央出现一个深色卡片节点 | — |
| 节点内容显示 Markdown | 看到带格式的文本（标题加粗） | — |

### 3.2 节点编辑

| 步骤 | 预期结果 | 实际 |
|------|---------|------|
| 双击节点 | 弹出编辑对话框（深色弹窗） | — |
| 「内容」Tab | 左侧 textarea 可写 Markdown，右侧实时预览 | — |
| 在 textarea 中输入 `# 测试` | 预览区显示加粗的"测试" | — |
| 「模板字段」Tab | 显示 6 行模板字段：责任人/任务/输入/输出/版本/状态 | — |
| 填一个字段值（如"张三"） | 字段在节点底部显示 | — |
| 点击「保存」 | 弹窗关闭，节点内容更新 | — |
| 点击「取消」 | 弹窗关闭，内容不变 | — |

### 3.3 箭头连接

| 步骤 | 预期结果 | 实际 |
|------|---------|------|
| 创建两个节点 | 画布上有两个可拖动的卡片 | — |
| 点击 tldraw 工具栏的箭头工具（→ 图标） | 鼠标变成十字 | — |
| 从一个节点拖到另一个节点 | 出现箭头连接线 | — |
| 双击箭头线 | 出现文本输入框，可输入文件名 | — |
| 输入文件名后回车 | 文件名显示在箭头中间 | — |
| 清空文件名 | 箭头标签消失（隐藏标签） | — |

### 3.4 拖拽排版

| 步骤 | 预期结果 | 实际 |
|------|---------|------|
| 拖拽节点 | 节点跟随鼠标移动 | — |
| 拖拽时连接的箭头 | 箭头自动跟随节点位置 | — |
| 缩放节点（拖右下角） | 节点大小变化，内容自适应 | — |

### 3.5 保存

| 步骤 | 预期结果 | 实际 |
|------|---------|------|
| 点击「💾 保存」 | 浏览器下载 `everything-flow-时间戳.json` | — |
| 用文本编辑器打开该 JSON | 包含 store 等结构数据 | — |

### 3.6 加载

| 步骤 | 预期结果 | 实际 |
|------|---------|------|
| 点击「📂 加载」 | 弹出文件选择对话框 | — |
| 选择之前保存的 JSON 文件 | 画布恢复到保存时的状态 | — |
| 选一个非 JSON 文件 | 弹窗提示"加载失败：JSON 格式无效" | — |
| 删除 JSON 中的 `store` 字段后加载 | 弹窗提示错误，画布不变 | — |

---

## 4. 容错验证

| 场景 | 预期 | 实际 |
|------|------|------|
| 画布加载时不断网 | 不白屏、无报错 | — |
| 坏 Markdown（如单独 `#`） | 降级显示纯文本，不 crash | — |
| 编辑器未就绪时点按钮 | 按钮灰色不可点，悬停显示"画布未就绪" | — |
| React 渲染异常 | Error Boundary 捕获，显示中文错误页 + 「重试」按钮 | — |
| 节点字段 JSON 损坏 | 字段区不显示，不 crash | — |

---

## 5. 已知限制（MVP）

- **需要 Node.js**：不是单 HTML，Vite 开发服务器必须运行
- **无持久化存储**：每次需手动保存/加载 JSON（除非浏览器缓存保持）
- **暗色主题仅限画布内部**：tldraw 的 UI 已有暗色，编辑弹窗独立暗色
- **无自动排版**：dagre 布局引擎未集成（MVP 范围外）
- **无网状图**：文件追踪的网状视图在后期开发
- **无 AI/MCP 控制**：预留接口后续接入

---

## 6. 文件结构

```
projects/everything-flow/
├── run.sh                  ← Linux 启动脚本
├── run.bat                 ← Windows 启动脚本
├── package.json            ← 依赖配置
├── vite.config.js          ← Vite 构建配置
├── index.html              ← 入口 HTML
├── src/
│   ├── main.jsx            ← React 入口
│   ├── App.jsx             ← 主组件（tldraw + 工具栏 + 编辑弹窗）
│   ├── App.css             ← 样式
│   ├── ErrorBoundary.jsx   ← React 错误边界
│   ├── shapes/
│   │   └── FlowNodeShapeUtil.jsx  ← 自定义节点 Shape
│   └── components/
│       └── NodeEditor.jsx  ← 节点编辑弹窗
└── docs/
    └── visual-file-flow-editor.md  ← 立项讨论记录
```

---

## 7. 快速操作参考

| 操作 | 方法 |
|------|------|
| 添加节点 | 工具栏「➕ 添加节点」 |
| 编辑节点 | 双击节点 |
| 连接节点 | 箭头工具拖拽 |
| 编辑箭头标签 | 双击箭头线 |
| 保存 | 💾 保存 |
| 加载 | 📂 加载 |
| 删除 | 选中后按 Delete 键 |
| 撤销 | Ctrl+Z |
| 重新加载 | Ctrl+F5（开发服务器热重载） |
