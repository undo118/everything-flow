# 2026-06-08 工作日志

## 今日事项

### Bug 修复
- **快捷键面板被节点连接点遮挡** — ConnectorOverlay (zIndex 9999) 盖住 tldraw 键盘快捷键弹窗，添加 `.tlui-dialog__overlay { z-index: 10010 !important; }` 修复

### Phase 6 — 页面管理（CustomPageSelector）✅
- 页面选择器按钮 + 下拉列表
- hover 操作：重命名/复制/删除
- 拖拽手柄（⠿）替代 ⬆⬇ 按钮，HTML5 drag & drop 排序
- 节点计数（只算 flow-node，不含 port）
- page.index 使用 `getIndices()` 从 `@tldraw/utils` 生成合法 index key

### Phase 7 — Undo/Redo 提取到顶部栏 ✅
- `UndoRedo` 组件，放在汉堡菜单右侧
- 使用 `editor.getCanUndo()` / `editor.getCanRedo()` 控制禁用态

### Phase 8 — 💾 导出下拉菜单 ✅
- `SaveExportMenu` 组件替换原来 💾 保存按钮
- 保存为 .json / SVG/PNG/JSON 导出 / 透明背景开关
- 使用 `window.__TLDRAW_ACTIONS` 调用 tldraw 导出 action

### 重大决策
- Phase 9（布局重排）和 Phase 10（最终收尾）跳过
- 「树状图布局」功能推迟，等正交连线做好后再做
- Plan `redesign-top-left-panel.md` 移入 `plans/completed/`

## 明天待办
- 树状图布局功能（dagre 自动布局）
- 或根据用户需求继续其他功能开发

## 笔记
- tldraw 2.x 的 `index` 字段使用专属 index key 格式（`a1`, `a2Vg`...），需用 `getIndices(n)` 生成
- `editor.setCurrentPage(pageId)` 而非 `setCurrentPageId`
- `editor.createPage({ name })` 需传对象参数
- HTML5 drag & drop 的 `draggable` 属性可单独设在手柄元素上，`onDragOver`/`onDrop` 在父元素处理
- Phase 9/10 用户决定跳过，当前 UI 状态已满足需求
