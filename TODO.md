# TODO — everything-flow

## P1 — 核心功能

- [ ] **节点样式多样化（按标准流程图种类）** — 支持标准流程图节点类型，如：
  - 开始/结束（圆角矩形/胶囊）
  - 处理（矩形）
  - 判断（菱形）
  - 子流程（双边矩形）
  - 文档（波浪底矩形）
  - 数据（平行四边形）
  - 每个类型有自己的图标、形状和默认配色

- [ ] **节点连接线方式（正交/直线）** — 箭头支持两种连线模式：
  - 正交连线：三段式横平竖直（像原理图走线）
  - 直线：当前已有的斜线直连
  - 每条连接线可单独切换，也可全局设置默认

- [ ] **节点分区分组及可视化** — 节点可以分组/分区（类似泳道或容器），组有背景色和标签，用于区分不同角色/阶段/模块

- [ ] **节点排版分布方式** — 支持一键排版布局：
  - 水平排列
  - 垂直排列
  - 自动布局（dagre 算法）

- [ ] **节点间对齐模式** — 拖拽时显示对齐参考线（vs code 风格），多选后支持对齐操作：左对齐、右对齐、水平居中、顶部对齐、底部对齐、垂直居中、等间距分布

## P2 — 视觉与样式

- [ ] **节点背景颜色** — 每个节点可单独设置背景色（颜色选择器）
- [ ] **节点框颜色** — 每个节点可单独设置边框颜色和粗细
- [ ] **更改背景样式** — 画布背景可切换：网格、点阵、纯色
- [ ] **节点默认设置** — 新建节点的默认样式配置（默认颜色、大小、字体、连线模式等），配置入口在工具栏

## P3 — 工程整洁

- [x] ~~删除备份文件 `src/App.jsx.bak.v0.1`~~
