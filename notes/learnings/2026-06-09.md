# 2026-06-09 工作日志

## 今日事项

### 箭头正交路由 — 完整实现 ✅
大量工作集中在箭头正交化上，从 HTML 原型验证到工程代码实现。

#### 架构改造
- **箭头从 overlay SVG 升级为 tldraw 原生 Shape**（尝试后回退）
- 最终方案：保留 overlay SVG + React div 作为交互层
- 新建 `src/utils/orthogonalRouting.js` — 正交路由算法

#### 对侧方向（bt/tb/lr/rl）
- 4 个方向独立实现，1段/3段/5段自适应
- 手柄拖拽支持（偏移量持久化）
- Bug 修复：movementY 改为 clientY 跟踪拖拽位移
- Bug 修复：手柄灵敏度参数化（0.4 默认值）

#### 同侧方向（tt/bb/ll/rr）
- A.t → B.t 通过 HTML 原型完整验证
- 3 段敞口型（无 X 重叠）/ 5 段 U 型（有 X 重叠）
- B 在上下方不同路由策略

#### 箭头交互
- 箭头选中（点击路径选中，高亮变色 + 箭头头变色）
- 箭头删除（捕获阶段拦截 Delete，stopPropagation 防止 tldraw 误删节点）
- 手柄 hover 反馈（悬停放大）
- 右键切换正交/直线模式
- 「全部正交」「全部直线」按钮（跳过已匹配的箭头）
- 默认正交/直线 toggle（偏好设置菜单，localStorage 持久化）
- 手柄灵敏度滑块（三级菜单，0.1~1.0）

#### 参考线对齐（手柄）
- 吸附到节点边线/中线 + 其他胶囊中心
- 松手吸附（拖拽时只显示引导线，鼠标释放才执行吸附）
- 吸附阈值 4px（松手时）
- 橙色虚线引导线，贯穿画布

#### 菜单系统改进
- 删除「编辑」菜单中的「树状图布局」
- 偏好设置增加「默认正交」toggle
- 手柄灵敏度三级子菜单

#### 节点拖拽跟随
- rAF 兜底确保箭头实时跟随节点移动
- 节点移动时重置关联箭头的手柄偏移

## 发现与教训
- SVG 元素在 `pointerEvents: none` 的容器内无法接收事件，必须用 React div 做交互层
- `e.movementY` 不可靠，改用 `clientY` 手动跟踪
- 3 段路径的 `pts` 必须返回 4 个点，否则 SVG 画成斜线
- Delete 箭头时需捕获阶段拦截事件 + stopPropagation 防止 tldraw 联动删除
- 吸附用「松手吸附」模式比实时吸附更自然，不会粘死
- 同侧路由与对侧路由逻辑不同，需要独立实现
- 直连线（2 点）的点击区域应沿线段旋转，而不是整体包围盒

## 明天待办
- 正交对角方向路由（top→left, bottom→right 等交叉方向）
- 框选同时选中箭头
- 箭头不盖住面板
- 直连线吸附范围 4px 体验确认

## 推荐事项
- bb/ll/rr 同侧方向也走 HTML 原型验证
- 手柄灵敏度持久化到 localStorage
